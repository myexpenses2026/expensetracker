_schema-version: 3.3.0
ID: expensetracker
description: Expense Tracker
version: 1.0.0
modules:
- name: expensetracker-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: expensetracker-auth
  - name: expensetracker-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: expensetracker-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: expensetracker-db
  parameters:
    buildpack: nodejs_buildpack
- name: expensetracker
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: expensetracker-auth
  - name: expensetracker-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: expensetracker-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: expensetracker-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - commyorgexpensetrackerexpenses.zip
      name: commyorgexpensetrackerexpenses
      target-path: resources/
    - artifacts:
      - commyorgexpensetrackerbudgetfpm.zip
      name: commyorgexpensetrackerbudgetfpm
      target-path: resources/
- name: commyorgexpensetrackerexpenses
  type: html5
  path: app/expenses
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: commyorgexpensetrackerbudgetfpm
  type: html5
  path: app/budgetfpm
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: expensetracker-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      role-collections:
      - description: generated
        name: Admin (expensetracker ${org}-${space})
        role-template-references:
        - $XSAPPNAME.Admin
      - description: generated
        name: User (expensetracker ${org}-${space})
        role-template-references:
        - $XSAPPNAME.User
      - description: generated
        name: userattributes (expensetracker ${org}-${space})
        role-template-references:
        - $XSAPPNAME.userattributes
      tenant-mode: dedicated
      xsappname: expensetracker-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: expensetracker-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: expensetracker-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: expensetracker-html5-service
    service-plan: app-host
- name: expensetracker-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: expensetracker-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
